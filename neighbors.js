/** MODULE INTERFACE
 *@method {function} - 
 */
module.exports = {
    createNeighbor: createNeighbor,
    createNeighborhood: createNeighborhood
};

/*----------------------------------------------------------------------------*/

/** 
 * @param
 * @returns
 */
function createNeighbor() {
    
}

/** 
 * @param {number} dimensions - The number of dimensions of the neighborhood model
 * @param {object} coordinates - An array containing the coordinate axes of the multidimensional space
 * @returns {object} neighborhood - 
 */
function createNeighborhood() {
    var neighborhood = {};

    return neighborhood;
}